[package]
name = "salvation-client"
version = "0.1.0"
edition = "2024"

[dependencies]
notify = "8"
tokio = { version = "1", features = ["full"]}
serde = { version = "1", features = ["derive"]}
serde_json = "1"
